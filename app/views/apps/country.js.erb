
alert("hi");

buildChart();

<% @ratings.each do |r| %>
		chartData['exp'].addRow([ "<%="#{r.app.name}"%>", new Date(<%=r.created_at.year%>,<%=r.created_at.month%>,<%=r.created_at.day%>), <%=r.rating_nice%>, <%=r.base%> ]) 		
<% end	%>

drawCharts();

